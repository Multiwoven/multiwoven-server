apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: multiwoven-server-claim0
  name: multiwoven-server-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
