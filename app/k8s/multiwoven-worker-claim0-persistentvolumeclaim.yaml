apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: multiwoven-worker-claim0
  name: multiwoven-worker-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
